import openpyxl
from os import path


def load_workbook(wb_path):
    if path.exists(wb_path):
        return openpyxl.load_workbook(wb_path)
    return openpyxl.Workbook()


wb_path = "userdata.xlsx"
wb = load_workbook(wb_path)

sheet = wb["userdata"]

if sheet.cell (row=1, column=1, value=Username) = True:
    exit()
else:
    sheet.insert_rows(1)
    for title in "Username":
        sheet.cell(row=1, column=1, value=title)

usernames = (1, 2, 3)

sheet.append(usernames)

# for username in "Username":
#     sheet.cell(row=1, column=1, value=username)


# for username in usernames:
#     sheet.append(username)

wb.save(wb_path)
